<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Pharmax - Gestion des Réclamations{% endblock %}</title>
    
    <!-- Google Fonts -->
    <link href='https://fonts.googleapis.com/css?family=Lato:400,400i,700,700i' rel='stylesheet' type='text/css'/>
    <link href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css' rel='stylesheet'/>
    <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet'>
    
    <style>
        /* Misho Template Colors - Soft Blue & Green Theme */
        :root {
            --main-color: #5B8DBE;
            --dark-color: #6BA873;
            --header-bg: #ffffff;
            --header-color: #222222;
            --menu-bg: #3D6B8C;
            --menu-color: #ffffff;
            --title-color: #2d4a5f;
            --body-bg: #f5f9fb;
            --body-text: #5a6b7d;
            --posts-bg: #ffffff;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Lato', sans-serif;
            background-color: var(--body-bg);
            color: var(--body-text);
            font-size: 14px;
        }
        
        /* Header */
        .misho-header {
            background-color: var(--header-bg);
            border-bottom: 1px solid #e0e0e0;
            padding: 20px 0;
            box-shadow: 0 2px 4px rgba(0,0,0,.05);
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .misho-header .logo {
            font-size: 28px;
            font-weight: 700;
            color: var(--main-color);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .misho-header .logo span {
            color: var(--dark-color);
        }

        .misho-header .logo-link {
            display: inline-block;
            max-height: 80px;
        }

        .misho-header .logo-img {
            height: 80px;
            width: auto;
            transition: transform 0.3s ease;
        }

        .misho-header .logo-link:hover .logo-img {
            transform: scale(1.05);
        }
        
        /* Navigation */
        .misho-nav {
            background-color: var(--menu-bg);
            padding: 0;
        }
        
        .misho-nav .nav-link {
            color: var(--menu-color) !important;
            padding: 15px 20px;
            font-weight: 500;
            border-bottom: 3px solid transparent;
            transition: all 0.3s ease;
        }
        
        .misho-nav .nav-link:hover,
        .misho-nav .nav-link.active {
            background-color: var(--main-color);
            border-bottom-color: var(--main-color);
        }
        
        /* Main Content */
        .misho-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }
        
        .misho-page-title {
            font-size: 32px;
            font-weight: 700;
            color: var(--title-color);
            margin-bottom: 30px;
            border-bottom: 3px solid var(--main-color);
            padding-bottom: 15px;
        }
        
        /* Cards */
        .misho-card {
            background-color: var(--posts-bg);
            border: none;
            border-radius: 2px;
            box-shadow: 0 1px 3px rgba(0,0,0,.1);
            margin-bottom: 25px;
            transition: box-shadow 0.3s ease;
        }
        
        .misho-card:hover {
            box-shadow: 0 3px 8px rgba(0,0,0,.15);
        }
        
        .misho-card-header {
            background-color: #f9f9f9;
            border-bottom: 1px solid #e0e0e0;
            padding: 20px;
        }
        
        .misho-card-header h5 {
            color: var(--title-color);
            font-weight: 700;
            margin: 0;
        }
        
        .misho-card-body {
            padding: 20px;
        }
        
        .misho-card-footer {
            background-color: #f9f9f9;
            border-top: 1px solid #e0e0e0;
            padding: 15px 20px;
        }
        
        /* Buttons */
        .btn-primary, .btn-success, .btn-danger {
            border-radius: 2px;
            font-weight: 600;
            padding: 10px 20px;
            border: none;
            transition: all 0.3s ease;
        }
        
        .btn-primary {
            background-color: var(--main-color);
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #4A7BA7;
            color: white;
        }
        
        /* Badges */
        .misho-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .badge-waiting {
            background-color: #ffc107;
            color: #333;
        }
        
        .badge-progress {
            background-color: #17a2b8;
            color: white;
        }
        
        .badge-resolved {
            background-color: #28a745;
            color: white;
        }
        
        /* Reclamation Grid */
        .reclamation-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }
        
        .reclamation-card {
            background-color: var(--posts-bg);
            border-radius: 2px;
            overflow: hidden;
            box-shadow: 0 1px 3px rgba(0,0,0,.1);
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
        }
        
        .reclamation-card:hover {
            box-shadow: 0 3px 10px rgba(0,0,0,.15);
            transform: translateY(-2px);
        }
        
        .reclamation-card-header {
            background-color: #f9f9f9;
            padding: 20px;
            border-bottom: 3px solid var(--main-color);
        }
        
        .reclamation-card-header h6 {
            margin: 0 0 5px 0;
            color: var(--title-color);
            font-weight: 700;
        }
        
        .reclamation-card-body {
            padding: 20px;
            flex-grow: 1;
        }
        
        .reclamation-card-body p {
            margin: 0;
            color: var(--body-text);
            font-size: 14px;
            line-height: 1.6;
        }
        
        .reclamation-card-footer {
            padding: 15px 20px;
            background-color: #f9f9f9;
            border-top: 1px solid #e0e0e0;
        }
        
        .reclamation-card-footer .btn-group {
            display: flex;
            gap: 8px;
            width: 100%;
        }
        
        .reclamation-card-footer .btn {
            flex: 1;
            padding: 8px 12px;
            font-size: 12px;
            font-weight: 600;
        }
        
        /* Form */
        .misho-form {
            background-color: var(--posts-bg);
            padding: 30px;
            border-radius: 2px;
            box-shadow: 0 1px 3px rgba(0,0,0,.1);
        }
        
        .misho-form input,
        .misho-form textarea,
        .misho-form select {
            border: 1px solid #ddd;
            border-radius: 2px;
            padding: 12px;
            font-family: 'Lato', sans-serif;
            font-size: 14px;
            transition: border-color 0.3s ease;
        }
        
        .misho-form input:focus,
        .misho-form textarea:focus,
        .misho-form select:focus {
            border-color: var(--main-color);
            outline: none;
            box-shadow: 0 0 0 3px rgba(255,0,0,.1);
        }
        
        .misho-form label {
            font-weight: 600;
            color: var(--title-color);
            margin-bottom: 8px;
            display: block;
        }
        
        /* Footer */
        .misho-footer {
            background-color: var(--menu-bg);
            color: var(--menu-color);
            padding: 40px 0;
            margin-top: 60px;
            text-align: center;
        }
        
        .misho-footer p {
            margin: 0;
        }
        
        /* Alert Messages */
        .alert {
            border-radius: 2px;
            border: none;
            border-left: 4px solid;
        }
        
        .alert-info {
            border-left-color: #17a2b8;
            background-color: #e7f3f7;
            color: #0c5460;
        }
        
        .alert-warning {
            border-left-color: #ffc107;
            background-color: #fff8e1;
            color: #856404;
        }
        
        .alert-success {
            border-left-color: #28a745;
            background-color: #e8f5e9;
            color: #155724;
        }
        
        /* Table */
        .misho-table {
            background-color: var(--posts-bg);
            border-collapse: collapse;
        }
        
        .misho-table thead {
            background-color: #f0f0f0;
            border-bottom: 2px solid var(--main-color);
        }
        
        .misho-table th {
            color: var(--title-color);
            font-weight: 700;
            padding: 15px;
            text-align: left;
        }
        
        .misho-table td {
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .misho-table tbody tr:hover {
            background-color: #f9f9f9;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .reclamation-grid {
                grid-template-columns: 1fr;
            }
            
            .misho-page-title {
                font-size: 24px;
            }
            
            .misho-container {
                margin: 20px auto;
            }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Header -->
    <div class="misho-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <a href="{{ path('home') }}" class="logo-link text-decoration-none">
                        <img src="{{ asset('assets/img/pharmax-logo.png') }}" alt="Pharmax Logo" class="logo-img" />
                    </a>
                </div>
                <div class="col-md-6 text-end">
                    <small style="color: var(--title-color);">Gestion des Réclamations</small>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Navigation -->
    <nav class="misho-nav navbar navbar-expand-lg">
        <div class="container">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home') }}">
                            <i class="fas fa-home"></i> Accueil
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('frontend_reclamation_index') }}">
                            <i class="fas fa-list"></i> Mes Réclamations
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('frontend_reclamation_new') }}">
                            <i class="fas fa-plus"></i> Nouvelle Réclamation
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_reclamation_index') }}">
                            <i class="fas fa-cogs"></i> Admin
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- Main Content -->
    <div class="misho-container">
        {% block content %}{% endblock %}
    </div>
    
    <!-- Footer -->
    <div class="misho-footer">
        <div class="container">
            <p>&copy; 2026 Pharmax - Tous droits réservés</p>
        </div>
    </div>
    
    <script src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js'></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
