{% extends 'base_simple.html.twig' %}

{% block title %}{{'Edit Comment - Pharmax'|trans}}{% endblock %}

{% block page_header %}
<div>
    <h1 class="mb-2">{{'Edit Comment'|trans}}</h1>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-0">
            <li class="breadcrumb-item"><a href="/">{{'Dashboard'|trans}}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_commentaire_index') }}">{{'Comments'|trans}}</a></li>
            <li class="breadcrumb-item active">{{'Edit'|trans}}</li>
        </ol>
    </nav>
</div>
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-8">
        {{ include('commentaire/_form.html.twig', {'button_label': ('Update Comment'|trans)}) }}
    </div>
</div>
<div class="row mt-4">
    <div class="col-lg-8">
        {{ include('commentaire/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
