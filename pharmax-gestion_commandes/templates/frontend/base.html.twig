<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Pharmax{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    
    <style>
        body { font-family: 'Lato', sans-serif; background: #f8f8f8; }
        nav { background: #fff !important; border-bottom: 1px solid #e0e0e0; }
        .navbar-brand { font-weight: 700; color: #222 !important; }
        .nav-link { color: #222 !important; font-weight: 500; }
        .nav-link:hover { color: #ff0000 !important; }
        .product-card { background: #fff; border: 1px solid #e0e0e0; transition: box-shadow 0.3s; }
        .product-card:hover { box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
        .product-price { color: #ff0000; font-weight: 700; }
        .btn-primary { background: #ff0000; border: none; }
        .btn-primary:hover { background: #cc0000; }
        footer { background: #222; color: #fff; margin-top: 60px; }
        .badge { background: #ff0000; }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">Pharmax</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_produit_index') }}">Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_panier_index') }}">
                            Panier
                            {% if app.request.session.get('panier')|length > 0 %}
                            <span class="badge">{{ app.request.session.get('panier')|length }}</span>
                            {% endif %}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_frontend_commande_index') }}">Commandes</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="py-4">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </main>

    <!-- Footer -->
    <footer class="py-5">
        <div class="container">
            <div class="row mb-4">
                <div class="col-md-6">
                    <h5>Pharmax</h5>
                    <p>Votre partenaire de confiance pour les meilleures solutions pharmaceutiques.</p>
                </div>
                <div class="col-md-3">
                    <h5>Liens</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_produit_index') }}" class="text-white-50">Produits</a></li>
                        <li><a href="{{ path('app_frontend_commande_index') }}" class="text-white-50">Commandes</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Contact</h5>
                    <p class="small text-white-50">contact@pharmax.tn<br>+216 XX XXX XXX</p>
                </div>
            </div>
            <hr class="bg-white-50">
            <p class="text-center text-white-50 small mb-0">&copy; 2026 Pharmax - Tous droits réservés</p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    {% block javascripts %}{% endblock %}
</body>
</html>
